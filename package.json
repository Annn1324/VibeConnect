{
  "name": "vibeconnect",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "server": "concurrently -n backend,frontend -c yellow,cyan \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "server:backend": "npm run dev --prefix backend",
    "server:frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
